# Database Package

Shared SQLite models, migrations, and repository operations will live here.

The live database is stored under `MEDIA_LIBRARY_ROOT`; only schema and migration
code belongs in Git.


# Storage Package

This package will resolve safe paths beneath `MEDIA_LIBRARY_ROOT`, validate that
the external drive is available, manage temporary files, and perform atomic moves.

Database records will contain relative paths rather than device-specific drive
letters.


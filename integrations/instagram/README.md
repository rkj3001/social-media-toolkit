# Instagram Integration

The Instagram integration will isolate source discovery and metadata mapping from
the rest of the application.

Before implementation, the available official API capabilities and permissions
must be confirmed for the intended public accounts. The integration will not
bypass authentication, platform controls, CAPTCHAs, or rate limits.

